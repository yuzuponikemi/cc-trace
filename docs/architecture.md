# cc-trace アーキテクチャ

## 全体像

```
┌─────────────────────┐
│  ~/.claude/projects/ │    セッションログの保存場所
│  ├── proj-A/         │    （Claude Code が自動生成）
│  │   ├── sess1.jsonl │
│  │   └── sess2.jsonl │
│  └── proj-B/         │
│      └── sess3.jsonl │
└────────┬────────────┘
         │
         │  sync.py が走査
         ▼
┌─────────────────────┐
│    Staleness Gate    │  最終更新から 300 秒以上経過？
│                      │  → No: スキップ（まだ書き込み中）
└────────┬────────────┘
         │ Yes
         ▼
┌─────────────────────┐
│   Dedup Gate         │  SHA-256 ハッシュが前回と同じ？
│   (state.json)       │  → Yes: スキップ（処理済み）
└────────┬────────────┘
         │ No (新規 or 変更あり)
         ▼
┌─────────────────────┐
│   parser.py          │  JSONL → Session データ構造
│                      │  ノイズ除去・ストリーム結合
└────────┬────────────┘
         │ Session
         ▼
┌─────────────────────┐
│   transformer.py     │  Session → Obsidian Markdown
│                      │  コードブロック抽象化
│                      │  ファイルパス抽出
└────────┬────────────┘
         │ Markdown 文字列
         ▼
┌─────────────────────┐
│   Obsidian Inbox     │  CC-2026-02-02-myapp-abc12345.md
│   (Google Drive)     │  として書き出し
└─────────────────────┘
```

## モジュール構成

```
src/cc_trace/
├── cli.py           エントリポイント。argparse でコマンドを振り分ける
├── config.py        設定の読み込み（TOML ファイル → CLI 引数の順で上書き）
├── parser.py        JSONL の解析。生ログを Session 構造体に変換
├── transformer.py   Session を Markdown テキストに変換
├── sync.py          ファイル走査・重複排除・書き出しのオーケストレーション
└── scheduler.py     crontab の登録・解除
```

## 処理フローの詳細

### 1. ソースログの形式

Claude Code は各セッションを JSONL（1 行 1 JSON）で記録します。

```
~/.claude/projects/-Users-ikmx-source-personal-myapp/
  └── a1b2c3d4-0000-0000-0000-000000000000.jsonl
```

ディレクトリ名はプロジェクトの絶対パスをハイフン区切りに変換したもの、ファイル名はセッション UUID です。

各行の `type` フィールドで種別が決まります。

| type | 内容 | cc-trace での扱い |
|---|---|---|
| `user` | ユーザーの入力 | `userType == "external"` のみ抽出 |
| `assistant` | Claude の応答（ストリーミングチャンク） | 同一ターンを結合して 1 メッセージに |
| `progress` | サブエージェントの進捗 | スキップ |
| `file-history-snapshot` | ファイル状態のスナップショット | スキップ |

### 2. parser.py: ログの構造化

`parse_session()` が JSONL を 1 行ずつ読み、`Session` オブジェクトを構築します。

```
JSONL の各行
  │
  ├─ type == "user" かつ userType == "external"
  │    → Message(role="user") を追加
  │    → sessionId, timestamp, content を抽出
  │
  ├─ type == "assistant"
  │    ├─ content block: "thinking"  → ContentBlock(type="thinking")
  │    ├─ content block: "text"      → ContentBlock(type="text")
  │    └─ content block: "tool_use"  → ContentBlock(type="tool_use")
  │
  │    ※ 直前も assistant なら同一メッセージに結合（ストリーミング対策）
  │    ※ tool_use が Read/Edit/Write/Glob/Grep ならファイルパスを収集
  │
  └─ それ以外 → スキップ
```

**ストリーミング結合**: Claude Code は 1 回の応答を複数の JSONL 行に分割して記録します（thinking → text → tool_use がそれぞれ別行）。parser は連続する `assistant` 行を 1 つの `Message` にマージします。

**データ構造**:

```
Session
├── session_id: str          セッション UUID
├── project: str             プロジェクト名（ディレクトリ末尾から推定）
├── model: str               使用モデル (例: claude-opus-4-5-20251101)
├── started_at: str          セッション開始時刻
├── total_input_tokens: int  入力トークン累計
├── total_output_tokens: int 出力トークン累計
├── related_files: list[str] 操作対象ファイル一覧
└── messages: list[Message]
    ├── role: str                "user" or "assistant"
    ├── timestamp: str
    └── content_blocks: list[ContentBlock]
        ├── type: str            "text" / "thinking" / "tool_use"
        ├── text: str            テキスト内容
        ├── tool_name: str       ツール名 (tool_use のみ)
        └── tool_input: dict     ツール引数 (tool_use のみ)
```

### 3. transformer.py: Markdown への変換

`transform_session()` が `Session` を Obsidian 向け Markdown に変換します。

#### Frontmatter

```yaml
---
created: 2026-02-02T06:08:52Z
tags:
  - log/claude
  - type/thought_trace
status: auto_generated
tokens: 25000
model: claude-opus-4-5-20251101
project: cc-trace
related_files:
  - src/main.py
---
```

`related_files` は parser が収集したファイルパスの一覧です。Obsidian の Dataview プラグインなどで「このセッションで触ったファイル」を検索できます。

#### コンテンツブロックの変換ルール

| ブロック種別 | 変換結果 |
|---|---|
| User text | `## 🧑 User` 見出しの下にそのまま表示 |
| Assistant thinking | `> [!thinking]- Thinking` Obsidian callout（折りたたみ） |
| Assistant text | `## 🤖 Assistant` 見出しの下に表示 |
| tool_use | `> 🔧 Used **ToolName**: 概要` の 1 行サマリー |

#### コードブロック抽象化

テキスト中のフェンスドコードブロックを検知し、プレースホルダーに置換します。

```
変換前:                              変換後:
```python                            [Code Block: python, 45 lines]
def calculate():
    ... (45行のコード)
```→
```

これにより、Markdown ファイルから実装の詳細が除去され、**思考の流れ**だけが残ります。

#### ツール使用の要約

ツール種別に応じて最も意味のある情報を 1 行に圧縮します。

| ツール | 表示される情報 |
|---|---|
| Read / Edit / Write | ファイルパス |
| Bash | description フィールド（なければコマンド先頭 60 文字） |
| Glob / Grep | 検索パターン |
| Task | サブエージェントの説明 |
| WebSearch | 検索クエリ |
| WebFetch | URL |

### 4. sync.py: 同期エンジン

`sync()` が全体のオーケストレーションを担います。

```
sync() の実行フロー
│
├── ~/.claude/projects/**/*.jsonl を再帰走査
│
└── 各ファイルに対して:
    │
    ├── [Staleness Gate] mtime チェック
    │   最終更新から staleness_threshold (300秒) 未満
    │   → スキップ（Claude Code がまだ書き込み中の可能性）
    │
    ├── [Dedup Gate] SHA-256 ハッシュを計算
    │   state.json に保存済みのハッシュと一致
    │   → スキップ（前回と同じ内容）
    │
    ├── parse_session() で Session に変換
    │   メッセージが 0 件 → スキップ（空セッション）
    │
    ├── transform_session() で Markdown に変換
    │
    ├── Obsidian Inbox に書き出し
    │   ファイル名: CC-{日付}-{プロジェクト}-{セッションID先頭8文字}.md
    │   同名ファイルが既にあれば上書き（セッション続行による更新）
    │
    └── state.json にハッシュを保存
```

#### State ファイル

`~/.claude/cc-trace-state.json` に処理済みファイルの SHA-256 ハッシュを記録します。

```json
{
  "/Users/ikmx/.claude/projects/proj-A/sess1.jsonl": "a1b2c3...",
  "/Users/ikmx/.claude/projects/proj-B/sess2.jsonl": "d4e5f6..."
}
```

同じファイルでもハッシュが変わっていれば再処理します。これはセッションが続行され、ログが追記されたケースに対応しています。

#### Staleness の意味

Claude Code は応答のストリーミング中にログファイルへ逐次書き込みます。書き込み途中のファイルを処理すると不完全な Markdown が生成されるため、最終更新から一定時間が経過するまで処理を保留します。

### 5. scheduler.py: 自動実行

crontab にエントリを追加・削除します。

```
0 * * * * /path/to/uv run cc-trace sync >> ~/.claude/cc-trace.log 2>&1 # cc-trace-auto
```

末尾の `# cc-trace-auto` がマーカーコメントです。install / uninstall はこのマーカーを基準にエントリを特定します。

## ファイルの流れまとめ

```
Claude Code セッション
        │
        ▼
~/.claude/projects/proj/session.jsonl   ← Claude Code が自動記録
        │
        ▼  cc-trace sync
~/.claude/cc-trace-state.json           ← 処理済みハッシュの記録
        │
        ▼
Obsidian Inbox/CC-2026-02-02-proj-abc12345.md  ← 思考ログ
```
